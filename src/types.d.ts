/**
 * @summary An object representing a month, where the keys are the day of the month, and
 * the values indicate whether that day is a workday
 */
export type MonthDays = { [key: number]: boolean };
